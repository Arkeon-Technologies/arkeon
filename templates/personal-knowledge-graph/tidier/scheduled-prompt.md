Check for recently created entities (concepts, observations, people) in space {{SPACE_ID}} since your last run. Compare their labels against all existing entities in the graph.

Merge any exact or near-duplicate labels you find — keep the older entity, merge the newer one into it. Delete any low-quality entities that add no value. Verify that recent entities have proper relationships (derived_from, mentioned_in, observed_in).

Report what you did with done().