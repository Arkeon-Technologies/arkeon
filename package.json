{
  "name": "arke-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test:e2e": "vitest run --config vitest.e2e.config.ts",
    "test:e2e:presigned": "E2E_PRESIGNED=1 vitest run --config vitest.e2e.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "aws4fetch": "^1.0.20",
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.77.0"
  }
}
